const string ='hello';
console.log(string.padStart(6, '_'));
console.log(string.padEnd(6, '_'))

/*
A variable string is initialized with a string value of 'hello'.
The first console.log() method pads the string to make sure that the length becomes 6 characters by adding _ in the beginning of the superfluous characters.
The second console.log() method pads the string to make sure that the length becomes 6 characters by adding _ in the end of the superfluous characters.
*/