const countries = {MX: 'Mexico', CO: 'Colombia', CL: 'Chile', PE: 'Peru'};
console.log(Object.values(countries));

/* 
The given code defines an object called countries containing key-value pairs that represent country codes and their names in string format.

The Object.values() method retrieves all the values of the provided countries object as an array, which is outputted as a result using console.log().

The output would be:

["Mexico", "Colombia", "Chile", "Peru"]
*/